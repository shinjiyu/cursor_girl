/**
 * 测试脚本：检查 OrtensiaClient 的发现对话功能
 */

// 在浏览器控制台中运行这个：

/*
// 1. 检查 OrtensiaClient 实例
const client = window.OrtensiaClient?.getInstance();
console.log('Client:', client);
console.log('Connected:', client?.isConnected());

// 2. 手动触发发现对话
if (client) {
  console.log('🔍 手动触发发现对话...');
  client.discoverExistingConversations();
  console.log('✅ 已发送 GET_CONVERSATION_ID 请求');
}

// 3. 检查 WebSocket 状态
console.log('WebSocket readyState:', client?.ws?.readyState);
console.log('WebSocket OPEN:', WebSocket.OPEN);
console.log('WebSocket CONNECTING:', WebSocket.CONNECTING);

// 4. 查看最近的消息
console.log('Recent messages processed:', client?.processedMessages?.size);
*/

export {}





















