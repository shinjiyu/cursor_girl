# Cursor Agent Hooks

## 📊 详细日志系统

所有 Agent Hooks 现在都配备了**详细的工作日志**，让你清楚地看到它们被 Cursor 调用时做了什么。

### 📝 日志内容

每次 Hook 被调用时，会记录：

1. **调用信息**
   - Hook 名称
   - 时间戳
   - Conversation ID
   - Generation ID
   - Cursor 版本
   - 工作区路径

2. **输入数据**
   - 完整的输入参数
   - 命令/文件/工具名称
   - 工作目录
   - 附加上下文

3. **处理过程**
   - 每个处理步骤
   - 条件判断
   - 模式匹配结果
   - 决策逻辑

4. **输出结果**
   - 权限决策（allow/deny/ask）
   - 用户消息
   - Agent 消息

5. **オルテンシア通知**
   - 发送的文本
   - 情绪类型
   - WebSocket 状态

6. **执行统计**
   - 执行耗时
   - 成功/失败状态

---

## 🔍 查看日志

### 方法 1: 使用日志查看工具（推荐）✨

```bash
cd ~/.cursor-agent
./view_logs.sh
```

功能：
- 📄 查看最新日志
- 📜 查看所有日志
- 👁️ 实时监控日志
- 🔍 搜索日志
- 📊 统计调用次数
- ⏱️ 查看最近一次调用
- 🧹 清空日志

### 方法 2: 直接查看日志文件

```bash
# 查看最新日志
tail -f /tmp/cursor-agent-hooks.log

# 查看最近 50 条
tail -50 /tmp/cursor-agent-hooks.log

# 搜索特定 Hook
grep "beforeShellExecution" /tmp/cursor-agent-hooks.log

# 搜索错误
grep "ERROR" /tmp/cursor-agent-hooks.log
```

---

## 📋 日志示例

### beforeShellExecution 日志

```
======================================================================
📥 [beforeShellExecution] 接收到 Cursor 调用
======================================================================
📋 输入数据摘要:
   • conversation_id: 43d1d8f4-d658-4aa7-8960-6ac89a31d186
   • generation_id: c815f15b-ecbf-4355-865e-569d17aa7ba8
   • command: npm build
   • cwd: /Users/user/project
   • hook_event_name: beforeShellExecution
   • cursor_version: 2.0.43
   • workspace_roots: ['/Users/user/project']
✅ 输入数据解析成功

⏳ 步骤 2/3: 执行 Hook 逻辑...
🔐 执行权限检查...
🔍 检查命令: npm build
📁 工作目录: /Users/user/project
🔍 步骤 1/3: 检查危险命令模式...
✅ 未检测到危险命令
🔍 步骤 2/3: 检查风险命令模式...
✅ 未检测到风险命令
🔍 步骤 3/3: 检查是否为重要命令...
📢 检测到重要命令: npm

💬 准备发送消息到オルテンシア:
   • 文本: Agent 正在执行：npm build...
   • 情绪: neutral
   • 事件类型: beforeShellExecution
   • WebSocket: ws://localhost:8000/ws
✅ 消息已发送到オルテンシア

✅ 允许执行命令
🔐 权限决策结果:
   • 决策: allow

📤 输出响应给 Cursor:
   • permission: allow

======================================================================
✅ [beforeShellExecution] Hook 执行成功
⏱️  执行耗时: 0.123 秒
======================================================================
```

### beforeShellExecution (危险命令) 日志

```
======================================================================
📥 [beforeShellExecution] 接收到 Cursor 调用
======================================================================
📋 输入数据摘要:
   • command: rm -rf /
   • cwd: /Users/user/project
✅ 输入数据解析成功

🔐 执行权限检查...
🔍 检查命令: rm -rf /
🔍 步骤 1/3: 检查危险命令模式...
🚨 匹配到危险命令模式: rm\s+-rf\s+/
🚫 拒绝执行命令: rm -rf /

💬 准备发送消息到オルテンシア:
   • 文本: 检测到危险命令！已阻止：rm -rf /
   • 情绪: angry
   • WebSocket: ws://localhost:8000/ws
✅ 消息已发送到オルテンシア

🔐 权限决策结果:
   • 决策: deny
   • 用户消息: 🚫 危险命令已被阻止：rm -rf /

📤 输出响应给 Cursor:
   • permission: deny
   • user_message: 🚫 危险命令已被阻止：rm -rf /

======================================================================
✅ [beforeShellExecution] Hook 执行成功
⏱️  执行耗时: 0.089 秒
======================================================================
```

---

## 🎯 使用场景

### 1. 调试 Hook 行为

当你在 Cursor 中看到 Hook 被调用时：

```bash
# 查看最近一次调用的详细信息
cd ~/.cursor-agent
./view_logs.sh
# 选择 6 (查看最近一次调用)
```

你会看到：
- ✅ Hook 接收到了什么数据
- ✅ Hook 做了什么决策
- ✅ Hook 发送了什么消息给オルテンシア
- ✅ Hook 返回了什么结果

### 2. 监控安全事件

```bash
# 实时监控危险命令拦截
tail -f /tmp/cursor-agent-hooks.log | grep -E "危险命令|风险命令"
```

### 3. 统计 Hook 使用情况

```bash
cd ~/.cursor-agent
./view_logs.sh
# 选择 5 (统计调用次数)
```

输出示例：
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Hook 名称                      调用次数
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
beforeShellExecution            45
afterShellExecution              42
beforeReadFile                   18
afterFileEdit                    12
beforeSubmitPrompt                8
afterAgentResponse                8
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

总调用次数: 133
成功: 131
失败: 2
```

### 4. 排查问题

如果オルテンシア没有响应：

```bash
# 检查 Hook 是否被调用
grep "接收到 Cursor 调用" /tmp/cursor-agent-hooks.log | tail -5

# 检查是否有错误
grep "ERROR" /tmp/cursor-agent-hooks.log

# 检查 WebSocket 发送
grep "消息已发送到オルテンシア" /tmp/cursor-agent-hooks.log | tail -5
```

---

## 🔧 日志级别

日志包含三个级别：

- **INFO** (绿色): 正常流程信息
- **WARNING** (黄色): 警告信息（如检测到风险命令）
- **ERROR** (红色): 错误信息

过滤特定级别：
```bash
grep "INFO" /tmp/cursor-agent-hooks.log    # 只看信息
grep "WARNING" /tmp/cursor-agent-hooks.log # 只看警告
grep "ERROR" /tmp/cursor-agent-hooks.log   # 只看错误
```

---

## 📊 日志格式

```
[时间戳] [级别] 消息内容
```

示例：
```
[2025-11-02 13:02:07,782] [INFO] 🔍 检查命令: npm build
```

---

## 🎨 日志图标说明

| 图标 | 含义 |
|-----|------|
| 🎣 | Hook 启动 |
| 📥 | 接收输入 |
| 📋 | 数据摘要 |
| ⏳ | 执行步骤 |
| 🔐 | 权限检查 |
| 🔍 | 条件判断 |
| ✅ | 成功/通过 |
| ⚠️ | 警告 |
| 🚨 | 危险 |
| 🚫 | 拒绝 |
| ❓ | 需要确认 |
| 💬 | 发送消息 |
| 📤 | 输出响应 |
| ⏱️ | 执行耗时 |
| ❌ | 失败/错误 |
| 📢 | 重要通知 |
| ℹ️ | 一般信息 |

---

## 🛠️ 自定义日志

如果你想修改日志格式或添加更多日志，编辑：

```
~/.cursor-agent/lib/agent_hook_handler.py
```

关键方法：
- `read_input()` - 输入日志
- `write_output()` - 输出日志
- `send_to_ortensia()` - WebSocket 日志
- `run()` - 执行流程日志

---

## 📚 相关命令

```bash
# 查看日志
tail -f /tmp/cursor-agent-hooks.log

# 使用日志工具
cd ~/.cursor-agent && ./view_logs.sh

# 测试 Hook
echo '{"command":"ls"}' | python3 ~/.cursor-agent/hooks/beforeShellExecution.py

# 清空日志
> /tmp/cursor-agent-hooks.log

# 统计日志行数
wc -l /tmp/cursor-agent-hooks.log

# 查看日志文件大小
ls -lh /tmp/cursor-agent-hooks.log
```

---

**日志文件位置**: `/tmp/cursor-agent-hooks.log`  
**日志查看工具**: `~/.cursor-agent/view_logs.sh`  
**版本**: 2.1.0 (with detailed logging)

